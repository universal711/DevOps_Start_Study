apiVersion: v1
kind: Service
metadata:
  name: visit-counter-service
  namespace: visit-counter
  labels:
    app: visit-counter
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: visit-counter
